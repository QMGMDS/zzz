---
description: 开发任务标准工作流（调研 → 清单 → 执行 → 审查打回）
argument-hint: [需求描述]
---

立即通过 Skill 工具加载 `dev-workflow`，将下面的用户需求作为输入，完整走一遍其定义的流程（加载 zzz-dev 文档 → 调研子 agent → 任务清单 → 执行子 agent → 审查与打回）：

$ARGUMENTS

若上方没有需求内容，先向用户询问本次要完成什么，再进入流程。
