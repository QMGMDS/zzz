# 调研派发单（子 agent · Explore）

> 用途：主 Agent 填写本单后，通过 Agent 工具以 `subagent_type: Explore` 派发。
> 填写原则：自包含——子 agent 看不到主会话的任何上下文，需求、范围、问题都要写全。占位符一律删除。

## 任务等级

{{L3 或 L4；L4 必须回答波及面问题，L3 可略}}

## 需求原文

{{用户需求的原文，含用户补充的所有细节}}

## 背景提示

{{从 zzz-dev 文档路由得到的模块提示，如：队伍模块的参考文档位于 .agents/skills/zzz-dev/references/ 下，可先读再查代码}}

## 调研范围

{{明确的目录/模块范围，如 Assets/_Scripts/Module/Team/；范围不清时写全仓搜索的入手点}}

## 必须回答的问题

1. 现有实现在哪里？（列出 文件:行号）
2. 相关的公开 API / 契约 / 数据结构是什么？
3. 谁在调用它们？本次改动会波及哪些调用方？
4. 有没有可复用的既有实现，避免重复造轮子？
5. （仅 L4）受影响的公开契约清单，以及每个契约的全部调用方（文件:行号）？
6. {{按本次需求补充的专属问题}}

## 输出要求

以结构化报告返回：逐题作答，所有结论附 `文件:行号` 证据。发现代码与 zzz-dev 文档描述不符之处单独列出，并标注是否阻塞本次改动（阻塞型 = 文档描述的 API/结构不存在且本次改动依赖它）。只读调研，不修改任何文件。
